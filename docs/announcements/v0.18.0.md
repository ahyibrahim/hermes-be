Hermes v0.18.0 is on the wire.

Rails collapse with icons. Create room expands when you want it. Online is a badge on the face, not a grey green guess. In-call collapses to mute, share, and hangup. Call settings holds the mic. A corner tab opens peers and the share preview. Screen share has a sound.
