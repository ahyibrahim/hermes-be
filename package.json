{
  "name": "hermes-be",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "tsx --test src/*.test.ts",
    "test:integration": "tsx test/integration/integration.spec.ts"
  },
  "//test": "npm test runs only self-contained node:test files under src/. It boots what it needs and uses temporary databases.",
  "//test:integration": "npm run test:integration is NOT part of npm test. It requires a hermes-be already listening on port 3456 (override with PORT) and targets the v0.6.0 API surface, so it fails against the current server by design. Point it at a throwaway HERMES_DB_PATH, never at real history.",
  "dependencies": {
    "@fastify/multipart": "^8.3.1",
    "@fastify/websocket": "^10.0.0",
    "better-sqlite3": "^11.0.0",
    "fastify": "^4.25.2",
    "tsx": "^4.19.0",
    "typescript": "^5.7.2"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.0.0",
    "@types/node": "^22.10.0",
    "@types/ws": "^8.1.0",
    "ws": "^8.18.0"
  }
}
