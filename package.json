{
  "name": "hermes-be",
    "version": "0.12.0",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "LOG_LEVEL=silent tsx --test src/*.test.ts",
    "test:integration": "LOG_LEVEL=silent tsx --test src/v06-api.test.ts src/v07-api.test.ts src/v09-api.test.ts src/v10-api.test.ts src/v11-api.test.ts"
  },
  "//test": "npm test runs only self-contained node:test files under src/. It boots what it needs and uses temporary databases. LOG_LEVEL=silent keeps Pino off so the TAP output stays readable; logger.test.ts overrides the level when it captures logs.",
  "//test:integration": "Runs src/v06-api.test.ts (also part of npm test). The old live-server script is a historical note in test/integration/integration.spec.ts.",
  "dependencies": {
    "@fastify/multipart": "^8.3.1",
    "@fastify/rate-limit": "^9.1.0",
    "@fastify/static": "^7.0.4",
    "@fastify/websocket": "^10.0.0",
    "argon2": "^0.45.1",
    "better-sqlite3": "^11.0.0",
    "fastify": "^4.25.2",
    "pino": "^9.14.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.2"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.0.0",
    "@types/node": "^22.10.0",
    "@types/ws": "^8.1.0",
    "pino-pretty": "^13.1.3",
    "ws": "^8.18.0"
  }
}
